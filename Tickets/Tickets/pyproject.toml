[project]
name = "ticket_workspace"
version = "0.0.1"
description = "Custom ticket workspace page for ERPNext/Frappe Cloud"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
